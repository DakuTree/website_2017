- if(resumeProfile.skills.length > 0) {
	//TODO: Add ability for each thing to have a note.
	- resumeProfile.skills.sort(function(a, b) {
	-	var dLevel = b.level - a.level;
	-	if(dLevel) return dLevel;
	-
	-	var dLevelDetailed = b.level_detailed - a.level_detailed;
	- 	return dLevelDetailed;
	- });

	- var columnSize = Math.ceil(resumeProfile.skills.length / 2);
	- var firstColumn = resumeProfile.skills.splice(0, columnSize);
	- var secondColumn = resumeProfile.skills;

	div(class="row")
		div(class="col-md-6")
			ul(class="no-bullets")
				each skill in firstColumn
					li
						span(class="ability-title")= skill.title
						span(class="ability-score")
							- var filled = new Array(skill.level);
							each tmp in filled
								span(class="glyphicon glyphicon-star filled")

							- if(skill.level < 5) {
							- var notFilled = new Array(5 - skill.level);
							each tmp in notFilled
								span(class="glyphicon glyphicon-star")
							- }
						
					
		- if(columnSize >= 1) {
		div(class="col-md-6")
			ul(class="no-bullets")
				each skill in secondColumn
					li
						span(class="ability-title")= skill.title
						span(class="ability-score")
							- var filled = new Array(skill.level);
							each tmp in filled
								span(class="glyphicon glyphicon-star filled")

							- if(skill.level < 5) {
							- var notFilled = new Array(5 - skill.level);
							each tmp in notFilled
								span(class="glyphicon glyphicon-star")
							- }
		- }

- } else {
div(class="alert alert-warning") No skills were found in this resume.
- }
